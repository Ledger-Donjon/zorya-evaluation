; BINSEC configuration for Tendermint v0.26.0 VerifyCommit voting-power overflow
; Target function: github.com/tendermint/tendermint/types.(*ValidatorSet).VerifyCommit
;
; Go 1.11 (stack-based ABI — arguments on the stack, not registers)
; Function signature:
;   func (vals *ValidatorSet) VerifyCommit(
;       chainID string, blockID BlockID, height int64, commit *Commit) error
;
; Stack layout at function entry (Go 1.11 stack-based ABI):
;   [RSP+0x08]  vals    *ValidatorSet   (receiver)
;   [RSP+0x10]  chainID string ptr
;   [RSP+0x18]  chainID string len
;   [RSP+0x20]  blockID BlockID (struct, multiple words)
;   ...         height  int64
;   ...         commit  *Commit

starting_address: <github.com/tendermint/tendermint/types.(*ValidatorSet).VerifyCommit>

; ── Symbolic inputs ──────────────────────────────────────────────────────────

; Symbolise vals->totalVotingPower (the int64 cached field in ValidatorSet).
; BINSEC explores paths where this field can take any int64 value.
; When vals is concrete (from GDB snapshot), we override the field symbolically.
vals_ptr := @[rsp + 8, 8]                          ; *ValidatorSet pointer
total_voting_power := nondet as int64              ; symbolic totalVotingPower

; Stub TotalVotingPower() to return our symbolic value directly,
; bypassing the validator iteration loop.
replace <github.com/tendermint/tendermint/types.(*ValidatorSet).TotalVotingPower> by
  rax := total_voting_power;
  return
end

; ── Runtime stubs ─────────────────────────────────────────────────────────────

; Prevent path cuts on Go runtime stack-growth calls
replace <runtime.morestack>          by return end
replace <runtime.morestack_noctxt>   by return end

; Stub fmt.Errorf to avoid exploring string formatting
replace <fmt.Errorf> by
  rax := 0x1;     ; non-nil error sentinel
  return
end

; ── Goal ──────────────────────────────────────────────────────────────────────
; We are looking for the path where:
;   talliedVotingPower (= 0, all precommits nil) > TotalVotingPower()*2/3
; which requires TotalVotingPower()*2 to overflow into a negative int64.
; BINSEC has no semantic oracle to detect this wrong return value.
; The function returns normally (nil error) — BINSEC sees no violation.
